/**
 * Trivial placeholder so the agent factory has something real to build and
 * test before any feature exists. Replace via /planner.
 */
export function add(a: number, b: number): number {
  return a + b
}
