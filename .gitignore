node_modules/
dist/
.cache/
.DS_Store
*.tsbuildinfo

# Stack caches (Vite/Vitest) — un-ignored caches dirty the tree and abort the
# implement station's preflight.
.vite/
coverage/

# Editor
.idea/

# Factory run artifacts stay on disk, out of git, so they do not trip the
# implement station's clean-tree preflight.
runs/

# Playwright MCP artifacts
.playwright-mcp/

# Local-only symlinks to external repos (e.g. the bootcamp factory workshop)
.external/
